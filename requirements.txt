# Python dependencies for the analysis pipeline.
# Minimum versions reflect the environment the notebooks were developed and
# tested on. Newer patch releases are expected to work; pin exact versions
# (== instead of >=) if you need a fully frozen environment for archival.

pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
patsy>=0.5.6
pingouin>=0.5.4
matplotlib>=3.7
seaborn>=0.12

# Notebook execution
jupyter>=1.0
ipykernel>=6.0
nbconvert>=7.0
